# 🎄 Игра "Тайный Санта" - Telegram Бот

**Версия:** 1.1  
**Автор:** Michael BAG  
**Telegram:** [@michaelbag](https://t.me/michaelbag)  
**E-mail:** mk@remark.pro

Telegram бот для организации игры "Тайный Санта" (Secret Santa). Позволяет создавать группы, добавлять участников и проводить случайный розыгрыш подарков.

## 📋 Возможности

- ✅ Создание групп для розыгрыша с уникальным кодом
- ✅ Вступление в группу по коду
- ✅ Выход из группы
- ✅ Установка имени участника
- ✅ Проведение розыгрыша (случайное распределение участников)
- ✅ Автоматическая рассылка результатов участникам
- ✅ Закрытие группы после розыгрыша
- ✅ Один владелец - одна активная группа

## 🛠 Технологии

- Python 3.14+
- Django 6.0
- python-telegram-bot 22.5
- SQLite (база данных по умолчанию)
- PostgreSQL (опционально)

## 📦 Установка

### 1. Клонирование репозитория

```bash
git clone <repository_url>
cd santa_game
```

### 2. Создание виртуальной среды

Виртуальная среда уже создана в директории `venv/`. Если нужно создать заново:

```bash
python3 -m venv venv
```

### 3. Активация виртуальной среды

**macOS/Linux:**
```bash
source venv/bin/activate
```

**Windows:**
```bash
venv\Scripts\activate
```

### 4. Установка зависимостей

```bash
pip install -r requirements.txt
```

### 5. Применение миграций

```bash
python manage.py migrate
```

### 6. Создание суперпользователя (опционально, для доступа к админке)

```bash
python manage.py createsuperuser
```

## 🐧 Установка на Ubuntu Server 24.04

Подробная инструкция по установке проекта на Ubuntu Server 24.04 в папку `/opt/santa_game` с PostgreSQL.

### Автоматическая установка (рекомендуется)

Используйте интерактивный скрипт установки, который автоматически настроит все необходимое:

```bash
# Клонируйте репозиторий сразу в /opt/santa_game
sudo git clone https://github.com/michaelbag/santa_game.git /opt/santa_game
cd /opt/santa_game

# Запустите скрипт установки с правами sudo
sudo ./install.sh
```

**Примечание:** Клонирование сразу в `/opt/santa_game` позволяет в дальнейшем обновлять проект через `git pull` без необходимости перемещения файлов.

Скрипт интерактивно запросит:
- Путь установки (по умолчанию: `/opt/santa_game`)
- Имя системного пользователя (по умолчанию: `santa_game`)
- Токен Telegram бота
- Параметры PostgreSQL (или выбор SQLite)
- Создание суперпользователя Django (опционально)
- Автоматический запуск сервиса

Скрипт автоматически:
- ✅ Проверит и установит необходимые зависимости
- ✅ Создаст системного пользователя
- ✅ Настроит PostgreSQL (если выбран)
- ✅ Клонирует репозиторий
- ✅ Создаст виртуальную среду
- ✅ Установит зависимости Python
- ✅ Создаст файл `.env` с настройками
- ✅ Применит миграции базы данных
- ✅ Создаст systemd сервис для бота
- ✅ Опционально создаст systemd сервис для Django Admin
- ✅ Запустит и включит автозапуск сервисов

### Ручная установка

Если вы предпочитаете установку вручную, следуйте инструкциям ниже:

### Предварительные требования

- Ubuntu Server 24.04
- Доступ с правами `sudo`
- Доступ к интернету для установки пакетов

### Шаг 1: Обновление системы

```bash
sudo apt update
sudo apt upgrade -y
```

### Шаг 2: Установка системных зависимостей

```bash
# Python 3.14+ и необходимые инструменты
sudo apt install -y python3.14 python3.14-venv python3-pip git

# PostgreSQL и клиентские библиотеки
sudo apt install -y postgresql postgresql-contrib libpq-dev

# Дополнительные зависимости для сборки пакетов
sudo apt install -y build-essential python3-dev
```

### Шаг 3: Создание системного пользователя

```bash
# Создаем пользователя santa_game без домашней директории
sudo useradd -r -s /bin/bash -d /opt/santa_game santa_game

# Создаем директорию для проекта
sudo mkdir -p /opt/santa_game
sudo chown santa_game:santa_game /opt/santa_game
```

### Шаг 4: Настройка PostgreSQL

```bash
# Переключаемся на пользователя postgres
sudo -u postgres psql

# В консоли PostgreSQL выполняем:
CREATE USER santa_game WITH PASSWORD 'ваш_надежный_пароль';
CREATE DATABASE santa_game OWNER santa_game;
GRANT ALL PRIVILEGES ON DATABASE santa_game TO santa_game;
\q
```

**Важно:** Замените `'ваш_надежный_пароль'` на надежный пароль и сохраните его для дальнейшей настройки.

### Шаг 5: Клонирование репозитория

```bash
# Переключаемся на пользователя santa_game
sudo -u santa_game bash

# Клонируем репозиторий сразу в /opt/santa_game
# Использование точки (.) клонирует репозиторий в текущую директорию
cd /opt/santa_game
git clone https://github.com/michaelbag/santa_game.git .

# Выходим из сессии пользователя santa_game
exit
```

### Шаг 6: Создание виртуальной среды и установка зависимостей

```bash
# Переключаемся на пользователя santa_game
sudo -u santa_game bash

cd /opt/santa_game

# Создаем виртуальную среду
python3.14 -m venv venv

# Активируем виртуальную среду
source venv/bin/activate

# Обновляем pip
pip install --upgrade pip

# Устанавливаем зависимости
pip install -r requirements.txt

# Выходим из сессии
exit
```

### Шаг 7: Настройка переменных окружения

```bash
# Создаем файл .env от имени пользователя santa_game
sudo -u santa_game bash -c 'cat > /opt/santa_game/.env << EOF
TELEGRAM_BOT_TOKEN=ваш_токен_бота_здесь
DB_ENGINE=postgresql
DB_NAME=santa_game
DB_USER=santa_game
DB_PASSWORD=ваш_надежный_пароль
DB_HOST=localhost
DB_PORT=5432
EOF'
```

**Важно:** 
- Замените `ваш_токен_бота_здесь` на реальный токен бота от BotFather
- Замените `ваш_надежный_пароль` на пароль, который вы использовали при создании пользователя PostgreSQL

### Шаг 8: Применение миграций

```bash
# Переключаемся на пользователя santa_game
sudo -u santa_game bash

cd /opt/santa_game
source venv/bin/activate

# Применяем миграции
python manage.py migrate

# Создаем суперпользователя (опционально, для админки)
python manage.py createsuperuser

# Выходим из сессии
exit
```

### Шаг 9: Создание systemd сервиса для автозапуска

```bash
# Копируем пример файла сервиса
sudo cp /opt/santa_game/santa-game-bot.service.example /etc/systemd/system/santa-game-bot.service

# Или создаем вручную
sudo nano /etc/systemd/system/santa-game-bot.service
```

Содержимое файла (также доступно в `santa-game-bot.service.example`):

```ini
[Unit]
Description=Santa Game Telegram Bot
After=network.target postgresql.service
Requires=postgresql.service

[Service]
Type=simple
User=santa_game
Group=santa_game
WorkingDirectory=/opt/santa_game
Environment="PATH=/opt/santa_game/venv/bin"
ExecStart=/opt/santa_game/venv/bin/python /opt/santa_game/manage.py runbot
Restart=always
RestartSec=10
StandardOutput=journal
StandardError=journal

[Install]
WantedBy=multi-user.target
```

Сохраните файл (Ctrl+O, Enter, Ctrl+X в nano).

### Шаг 10: Запуск и настройка автозапуска

```bash
# Перезагружаем systemd для загрузки нового сервиса
sudo systemctl daemon-reload

# Включаем автозапуск при загрузке системы
sudo systemctl enable santa-game-bot.service

# Запускаем сервис
sudo systemctl start santa-game-bot.service

# Проверяем статус
sudo systemctl status santa-game-bot.service
```

### Управление сервисом

```bash
# Остановить бота
sudo systemctl stop santa-game-bot.service

# Запустить бота
sudo systemctl start santa-game-bot.service

# Перезапустить бота
sudo systemctl restart santa-game-bot.service

# Просмотр логов
sudo journalctl -u santa-game-bot.service -f

# Просмотр последних 100 строк логов
sudo journalctl -u santa-game-bot.service -n 100
```

### Управление DEBUG режимом Django

Для управления DEBUG режимом в Django админке используйте команду `toggle_debug`:

```bash
# Переключиться на пользователя santa_game
sudo -u santa_game bash

cd /opt/santa_game
source venv/bin/activate

# Показать текущий статус DEBUG режима
python manage.py toggle_debug --status

# Включить DEBUG режим
python manage.py toggle_debug --on

# Отключить DEBUG режим (рекомендуется для продакшена)
python manage.py toggle_debug --off

# Переключить DEBUG режим (включить, если выключен, и наоборот)
python manage.py toggle_debug

# Выходим из сессии
exit
```

**Важно:** 
- DEBUG режим должен быть **отключен** (`DEBUG = False`) в продакшене для безопасности
- После изменения DEBUG режима перезапустите сервис Django Admin:
  ```bash
  sudo systemctl restart santa-game-admin.service
  ```

### Обновление проекта

Для обновления проекта до последней версии:

```bash
# Переключаемся на пользователя santa_game
sudo -u santa_game bash

cd /opt/santa_game

# Останавливаем бота (из другой сессии или в новом терминале)
# sudo systemctl stop santa-game-bot.service

# Обновляем код из репозитория
git fetch origin
git pull origin main

# Активируем виртуальную среду
source venv/bin/activate

# Обновляем зависимости (если requirements.txt изменился)
pip install -r requirements.txt

# Применяем новые миграции (если есть)
python manage.py migrate

# Выходим из сессии
exit

# Перезапускаем сервисы
sudo systemctl restart santa-game-bot.service
# Если используется Django Admin:
# sudo systemctl restart santa-game-admin.service
```

**Примечание:** Благодаря тому, что репозиторий был клонирован сразу в `/opt/santa_game`, обновление выполняется простой командой `git pull`.

### Проверка работы

1. **Проверка статуса сервиса:**
   ```bash
   sudo systemctl status santa-game-bot.service
   ```

2. **Проверка подключения к базе данных:**
   ```bash
   sudo -u santa_game bash
   cd /opt/santa_game
   source venv/bin/activate
   python manage.py dbshell
   # Должна открыться консоль PostgreSQL
   \q
   exit
   ```

3. **Проверка логов:**
   ```bash
   sudo journalctl -u santa-game-bot.service -n 50
   ```

### Безопасность

1. **Права доступа к файлам:**
   ```bash
   # Убедитесь, что только пользователь santa_game имеет доступ
   sudo chown -R santa_game:santa_game /opt/santa_game
   sudo chmod 700 /opt/santa_game
   sudo chmod 600 /opt/santa_game/.env
   ```

2. **Firewall (если используется):**
   ```bash
   # Бот не требует входящих подключений, только исходящие к Telegram API
   # Никаких правил firewall не требуется
   ```

3. **Резервное копирование базы данных:**
   ```bash
   # Копируем пример скрипта резервного копирования
   sudo cp /opt/santa_game/backup_db.sh.example /opt/santa_game/backup_db.sh
   sudo chmod +x /opt/santa_game/backup_db.sh
   sudo chown santa_game:santa_game /opt/santa_game/backup_db.sh
   
   # Добавляем в crontab для ежедневного резервного копирования в 3:00
   sudo crontab -u santa_game -e
   # Добавьте строку:
   # 0 3 * * * /opt/santa_game/backup_db.sh
   ```
   
   Пример скрипта также доступен в файле `backup_db.sh.example`.

## 🚀 Запуск

### Получение токена бота

1. Найдите [@BotFather](https://t.me/BotFather) в Telegram
2. Отправьте команду `/newbot`
3. Следуйте инструкциям для создания бота
4. Скопируйте полученный токен

### Запуск бота

**Способ 1: Через переменную окружения (рекомендуется)**

```bash
export TELEGRAM_BOT_TOKEN="ваш_токен_бота"
python manage.py runbot
```

**Способ 2: Через аргумент команды**

```bash
python manage.py runbot --token ваш_токен_бота
```

**Способ 3: Через settings.py**

Отредактируйте `santagame/settings.py` и установите:
```python
TELEGRAM_BOT_TOKEN = "ваш_токен_бота"
```

Затем запустите:
```bash
python manage.py runbot
```

## 📱 Использование бота

### Команды бота

- `/start` - Начать работу с ботом
- `/help` - Показать справку
- `/create_group` - Создать новую группу
- `/join_group` - Вступить в группу по коду
- `/leave_group` - Выйти из группы
- `/my_groups` - Показать все ваши группы
- `/set_name` - Установить ваше имя в группе
- `/draw` - Провести розыгрыш (только для владельца группы)

### Как это работает

1. **Создание группы:**
   - Владелец использует `/create_group`
   - Вводит название группы
   - Вводит описание подарка (сумма, характер и т.д.)
   - Получает уникальный код группы
   - Владелец автоматически добавляется в группу

2. **Вступление в группу:**
   - Участник использует `/join_group`
   - Вводит код группы
   - Автоматически добавляется в группу

3. **Установка имени:**
   - Участник использует `/set_name`
   - Вводит своё имя для группы

4. **Проведение розыгрыша:**
   - Владелец использует `/draw` (минимум 2 участника)
   - Бот случайным образом распределяет участников
   - Все участники получают сообщение с именем получателя подарка
   - Группа закрывается

5. **Создание новой группы:**
   - После розыгрыша владелец может создать новую группу

## 🗄 База данных

Проект поддерживает **SQLite** (по умолчанию) и **PostgreSQL**.

### SQLite (по умолчанию)

База данных создается автоматически в файле `db.sqlite3` при первом запуске миграций. Подходит для разработки и небольших проектов.

### PostgreSQL

Для использования PostgreSQL установите следующие переменные окружения:

```bash
export DB_ENGINE=postgresql
export DB_NAME=your_database_name
export DB_USER=your_database_user
export DB_PASSWORD=your_database_password
export DB_HOST=localhost
export DB_PORT=5432
```

Или создайте файл `.env` в корне проекта (см. пример в `.env.example`):

```env
DB_ENGINE=postgresql
DB_NAME=santa_game
DB_USER=postgres
DB_PASSWORD=your_password
DB_HOST=localhost
DB_PORT=5432
```

**Примечание:** Проект автоматически загружает переменные из файла `.env` (если он существует) благодаря `python-dotenv`.

**Установка PostgreSQL:**

```bash
# macOS (через Homebrew)
brew install postgresql
brew services start postgresql

# Ubuntu/Debian
sudo apt-get install postgresql postgresql-contrib
sudo systemctl start postgresql

# Создание базы данных
createdb santa_game
```

**Применение миграций для PostgreSQL:**

```bash
python manage.py migrate
```

### Модели данных

- **TelegramUser** - Пользователи Telegram
- **Group** - Группы для розыгрыша
- **Participant** - Участники групп
- **Draw** - Результаты розыгрышей

### Хранение картинок подарков

**Важно:** Картинки подарков не хранятся на сервере как файлы. Вместо этого бот использует `file_id` от Telegram Bot API:

- Когда пользователь отправляет фото боту, Telegram сохраняет файл на своих серверах
- Бот получает уникальный `file_id` для этого файла
- `file_id` сохраняется в базе данных в поле `gift_photo_file_id` модели `Participant`
- При отправке подарка получателю бот использует `file_id` для получения файла с серверов Telegram

**Преимущества:**
- Не требуется хранилище для файлов на сервере
- Нет необходимости в настройке медиа-файлов Django
- Файлы автоматически доступны через Telegram API
- Экономия дискового пространства

**Ограничения:**
- `file_id` действителен только для конкретного бота
- Файлы хранятся на серверах Telegram (срок хранения зависит от политики Telegram)

### Доступ к админке Django

**Локальная разработка:**
```bash
python manage.py runserver
```

Откройте в браузере: http://127.0.0.1:8000/admin/

**На сервере (через systemd):**

Если при установке был создан сервис `santa-game-admin.service`:

```bash
# Статус сервиса
sudo systemctl status santa-game-admin.service

# Запуск/остановка
sudo systemctl start santa-game-admin.service
sudo systemctl stop santa-game-admin.service

# Просмотр логов
sudo journalctl -u santa-game-admin.service -f
```

Админка будет доступна по адресу: `http://IP_СЕРВЕРА:ПОРТ/admin/`

**Примечание:** По умолчанию Django Admin не создается автоматически. При установке через `install.sh` вы можете выбрать создание отдельного systemd сервиса для админки.

### Настройка Nginx с HTTPS (рекомендуется для продакшена)

Для безопасного доступа к админке через HTTPS рекомендуется использовать Nginx в качестве reverse proxy.

#### Предварительные требования

1. Установите Nginx:
   ```bash
   sudo apt install nginx
   ```

2. Установите SSL сертификат (Let's Encrypt):
   ```bash
   sudo apt install certbot python3-certbot-nginx
   sudo certbot --nginx -d example.com -d www.example.com
   ```

#### Настройка Nginx

1. **Скопируйте пример конфигурации:**
   ```bash
   sudo cp /opt/santa_game/nginx-santa-game-admin.conf.example /etc/nginx/sites-available/santa-game-admin
   ```

2. **Отредактируйте конфигурацию:**
   ```bash
   sudo nano /etc/nginx/sites-available/santa-game-admin
   ```

   Замените в файле:
   - `example.com` на ваш домен
   - `8000` на порт, на котором работает Django Admin (если отличается от стандартного)
   - Пути к SSL сертификатам (если используете не Let's Encrypt)

3. **Активируйте конфигурацию:**
   ```bash
   sudo ln -s /etc/nginx/sites-available/santa-game-admin /etc/nginx/sites-enabled/
   ```

4. **Проверьте конфигурацию:**
   ```bash
   sudo nginx -t
   ```

5. **Перезагрузите Nginx:**
   ```bash
   sudo systemctl reload nginx
   ```

#### Важные настройки в конфигурации

- **Порт Django Admin:** Убедитесь, что порт в `proxy_pass` совпадает с портом, на котором запущен сервис `santa-game-admin.service`
- **SSL сертификаты:** Обновите пути к сертификатам, если используете другой источник
- **Статические файлы:** Если используете статические файлы Django, настройте `collectstatic` и укажите правильный путь в `location /static/`

#### Настройка Django для работы за прокси

Убедитесь, что в `santagame/settings.py` настроены:

```python
# Для работы за Nginx reverse proxy
ALLOWED_HOSTS = ['example.com', 'www.example.com', 'localhost', '127.0.0.1']

# Если используете HTTPS через Nginx
SECURE_PROXY_SSL_HEADER = ('HTTP_X_FORWARDED_PROTO', 'https')
```

**Примечание:** Эти настройки уже добавлены в `settings.py` в виде комментариев. Раскомментируйте их при необходимости.

#### Обновление SSL сертификата

Let's Encrypt сертификаты действительны 90 дней. Для автоматического обновления:

```bash
# Проверка автоматического обновления
sudo certbot renew --dry-run

# Автоматическое обновление уже настроено через systemd timer
```

#### Проверка работы

После настройки админка должна быть доступна по адресу:
- `https://example.com/admin/` (через Nginx)
- `http://localhost:8000/admin/` (напрямую, если сервис запущен)

**Примечание:** Пример конфигурации Nginx находится в файле `nginx-santa-game-admin.conf.example` в корне проекта.

## 📁 Структура проекта

```
santa_game/
├── bot/                    # Приложение бота
│   ├── management/
│   │   └── commands/
│   │       └── runbot.py   # Команда запуска бота
│   ├── migrations/         # Миграции базы данных
│   ├── admin.py           # Настройки админки
│   ├── bot_handler.py     # Обработчики команд бота
│   └── models.py          # Модели данных
├── santagame/             # Настройки Django проекта
│   ├── settings.py
│   ├── urls.py
│   └── wsgi.py
├── venv/                          # Виртуальная среда
├── db.sqlite3                     # База данных (создается автоматически)
├── manage.py
├── requirements.txt               # Зависимости проекта
├── .gitignore
├── .cursorrules                   # Правила проекта
├── .env.example                   # Пример файла переменных окружения
├── install.sh                     # Скрипт автоматической установки
├── santa-game-bot.service.example  # Пример systemd сервиса
├── nginx-santa-game-admin.conf.example  # Пример конфигурации Nginx
├── backup_db.sh.example           # Пример скрипта резервного копирования
└── README.md
```

## ⚠️ Важные замечания

1. **Виртуальная среда:** Все Python скрипты должны запускаться из активированной виртуальной среды (см. `.cursorrules`)

2. **Токен бота:** Никогда не публикуйте токен бота в открытом доступе. Используйте переменные окружения или файлы конфигурации, которые не попадают в git.

3. **База данных:** SQLite используется по умолчанию и подходит для разработки. Для продакшена рекомендуется использовать PostgreSQL (см. раздел "База данных" выше).

4. **Безопасность:** Для продакшена установите `DEBUG = False` в `settings.py` и настройте `ALLOWED_HOSTS`.

## 🐛 Решение проблем

### Бот не отвечает

- Проверьте, что токен бота правильный
- Убедитесь, что бот запущен (`python manage.py runbot`)
- Проверьте логи на наличие ошибок

### Ошибки миграций

```bash
python manage.py makemigrations
python manage.py migrate
```

### Проблемы с виртуальной средой

Убедитесь, что виртуальная среда активирована:
```bash
which python  # Должен показать путь к venv/bin/python
```

## 📝 Лицензия

Этот проект создан в образовательных целях.

## 👨‍💻 Разработка

Для разработки рекомендуется:

1. Использовать виртуальную среду
2. Следовать PEP 8
3. Писать тесты для новой функциональности
4. Обновлять README при добавлении новых функций

## 📞 Поддержка

При возникновении проблем создайте issue в репозитории проекта.

